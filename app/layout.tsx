import type { Metadata } from 'next';
import { Gloock, Kalnia } from 'next/font/google';
import localFont from 'next/font/local';
import './globals.css';
import Background from './components/background';
import Navbar from './components/navbar';
import Headers from './components/headers';
import Footer from './components/footer';

const gloockSerif = Gloock({
  variable: '--font-gloock-serif',
  subsets: ['latin'],
  weight: '400',
});

const kalniaSerif = Kalnia({
  variable: '--font-kalnia-serif',
  weight: ['200', '400', '700'],
});

const gowooonSerif = localFont({
  src: [
    {
      path: '../public/fonts/GowunBatang-Regular.ttf',
      weight: '400',
    },
    {
      path: '../public/fonts/GowunBatang-Bold.ttf',
      weight: '700',
    },
  ],
  variable: '--font-gowoon-serif',
});

export const metadata: Metadata = {
  title: 'Create Next App',
  description: 'Generated by create next app',
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${gowooonSerif.variable} ${gloockSerif.variable} ${kalniaSerif.variable} antialiased h-screen flex flex-col items-center`}
      >
        <Headers />
        <section className="w-3/4 grow min-h-0 grid grid-cols-6 font-serif">
          <Navbar />
          <main className="col-span-5 overflow-y-scroll">{children}</main>
        </section>
        <Footer />
        <Background />
      </body>
    </html>
  );
}
